# T-1: 实现 src/codesense_v1/tools/add.py

## 任务背景

`tools` 包是工具实现的"插件目录"，每个 `.py` 文件实现 1 个工具，通过 `@tool` 装饰器自注册到 `registry`。MVP 阶段只有 `add`。

设计原则：
- 工具体只关心业务逻辑，**不**触碰 MCP 类型、JSON 协议、传输细节
- 入参用关键字参数接收，与 schema `properties` 名称一一对应
- 输出必须为 `str`（registry 包装为 `TextContent`）

### 已有依赖（可直接 import）

- `codesense_v1.errors`：已实现 `InvalidArgumentError`（继承自 `ToolError`），构造签名 `InvalidArgumentError(message: str)`
- `codesense_v1.schemas`：已声明 `ADD_INPUT_SCHEMA: Final[dict]`，结构：
  ```python
  {
      "type": "object",
      "properties": {
          "a": {"type": "number", "description": "加数 a"},
          "b": {"type": "number", "description": "加数 b"},
      },
      "required": ["a", "b"],
      "additionalProperties": False,
  }
  ```
- `codesense_v1.registry`：已实现 `tool(name, description, input_schema)` 装饰器，签名 `(name: str, description: str, input_schema: dict) -> Callable[[ToolHandler], ToolHandler]`；同名重复注册抛 `RuntimeError`

### 严禁

- import `codesense_v1.server`（避免循环）
- raise `ValidationError`（专属 registry 校验阶段）
- 捕获自身抛的异常自包装为 `CallToolResult`（由 registry 统一处理）

---

## 实现目标

实现 `add(a, b)` 工具：接收两个数，做 NaN/Infinity 自检 → 求和 → 结果非有限自检 → 返回 `str(a + b)`。

---

## 需要实现的文件

- `src/codesense_v1/tools/add.py`

不得创建 `src/codesense_v1/tools/__init__.py`（属于后续 T-2 任务）。

---

## 接口契约

```python
from codesense_v1.errors import InvalidArgumentError
from codesense_v1.registry import tool
from codesense_v1.schemas import ADD_INPUT_SCHEMA


@tool(
    name="add",
    description="计算两个数的和并返回字符串结果。",
    input_schema=ADD_INPUT_SCHEMA,
)
def add(a: float, b: float) -> str: ...
```

### 类型注解要点

- 参数 `a: float`、`b: float`（Python `int` 是 `float` 兼容值；JSON `number` 涵盖二者）
- 返回 `str`
- 同步函数（无 IO，不需要 async）

---

## 行为契约

### 自检（schema 已保证必填齐全、类型为 number、无多余参数，但仍要防御）

| 条件 | 抛出 | 文案 |
|------|------|------|
| `math.isnan(a)` | `InvalidArgumentError` | `"参数错误：'a' 不能为 NaN"` |
| `math.isnan(b)` | `InvalidArgumentError` | `"参数错误：'b' 不能为 NaN"` |
| `math.isinf(a)` | `InvalidArgumentError` | `"参数错误：'a' 不能为 Infinity"` |
| `math.isinf(b)` | `InvalidArgumentError` | `"参数错误：'b' 不能为 Infinity"` |
| 结果非有限（`a + b` 溢出为 inf 或 nan） | `InvalidArgumentError` | `"参数错误：结果溢出或非有限数"` |

取第一个不合法的参数报错（按 a → b 顺序，NaN 优先于 Infinity 检测）。

### 主流程伪代码

```text
def add(a, b):
    for name, v in (("a", a), ("b", b)):
        if math.isnan(v):
            raise InvalidArgumentError(f"参数错误：'{name}' 不能为 NaN")
        if math.isinf(v):
            raise InvalidArgumentError(f"参数错误：'{name}' 不能为 Infinity")

    result = a + b
    if not math.isfinite(result):
        raise InvalidArgumentError("参数错误：结果溢出或非有限数")

    return str(result)
```

### 返回值格式约定（保留浮点尾零，区分整数与浮点）

- `add(3, 5)` → `"8"`（两个 int 相加，结果 int，输出 `"8"`）
- `add(1.5, 2.5)` → `"4.0"`（任一为浮点则结果为浮点）
- `add(1.5, 1.0)` → `"2.5"`
- `add(3, 5.0)` → `"8.0"`

实现细节：直接 `return str(a + b)`，Python 原生 `str(float)` 自带 `.0` 后缀，`str(int)` 不带。

---

## 验收标准

- `from codesense_v1.tools.add import add` 成功
- `import` 触发 `add` 注册到 `registry._REGISTRY`，键 `"add"` 存在
- `add(3, 5) == "8"`、`add(1.5, 2.5) == "4.0"`
- `add(float('nan'), 1)` 抛 `InvalidArgumentError`，`.message == "参数错误：'a' 不能为 NaN"`
- 严禁 import `server`，严禁 raise `ValidationError`
- 全部公开符号带完整类型注解
- `uv run mypy --strict src/codesense_v1/tools/add.py` 零错误
- `uv run ruff check src/codesense_v1/tools/add.py` 零警告
- 单元测试由 TS-2 任务完成，本任务**不**写测试

---

## 范围约束

- **仅** 创建 `src/codesense_v1/tools/add.py`
- 严禁修改 `errors.py`、`schemas.py`、`registry.py`、`pyproject.toml`
- 严禁创建 `tools/__init__.py`
